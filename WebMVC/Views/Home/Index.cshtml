@{
    ViewData["Title"] = "Home Page";
}


<h3> Đang ở trang  home/index </h3>

@*<div class="p-4">
    <div class="row mb-2">
        <div class="cell-md-8 my-search-wrapper"></div>
        <div class="cell-md-4 my-rows-wrapper"></div>
    </div>
    <table class="table table-border cell-border" data-role="table"
           data-search-wrapper=".my-search-wrapper"
           data-rows-wrapper=".my-rows-wrapper"
           data-horizontal-scroll="true">

        <thead>
            <tr>
                @foreach (DataColumn col in Model.Columns)

                <th>Công cụ</th>
                <th>User Name</th>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Email</th>
                <th>Trạng thái</th>
                <th>Ngày tạo</th>
                <th>Ngày sửa</th>

            </tr>
        </thead>
        <tbody>
            @foreach (DataRow row in Model.Rows)
            {
            <tr>
                @foreach (DataColumn col in Model.Columns)

                <td>
                    <div class="multi-action d-flex flex-align-center">
                        <button class="action-button bg-amber bg-blue-hover fg-white second"
                                onclick="$(this).toggleClass('active')">
                            <span class="icon"><span class="mif-wrench"></span></span>
                        </button>
                        <ul class="actions drop-right">
                            <li class="bg-blue"><a href="#"><span class="mif-info"></span></a></li>
                            <li class="bg-warning"><a href="#"><span class="mif-pencil"></span>Edit</a></li>
                            <li class="bg-red"><a href="#"><span class="mif-cancel"></span>Delete</a></li>
                        </ul>
                    </div>
                </td>
                <td>@row[0]</td>
                <td>@row[1]</td>
                <td>@row[2]</td>
                <td>@row[3]</td>
                <td>@row[4]</td>
                <td>@row[5]</td>
                <td>@row[6]</td>
            </tr>

            }
        </tbody>
    </table>
</div>*@